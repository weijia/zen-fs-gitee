import type { Memory } from './memory.js';
import { type Type, type Value } from './types.js';
export interface PointerJSON {
    typename: string;
    address: number;
}
/**
 * A pointer
 */
export declare class Pointer<const T extends Type<any> = Type> {
    #private;
    type: T;
    readonly memory: Memory<ArrayBufferLike>;
    static readonly name = "Pointer";
    static size: number;
    static get(this: void, buffer: ArrayBufferLike, offset: number): Pointer<any>;
    static set(this: void, buffer: ArrayBufferLike, offset: number, value: Pointer<any>): void;
    constructor(type: T, value: number, memory?: Memory<ArrayBufferLike>);
    toString(): string;
    toJSON(): PointerJSON;
    valueOf(): number;
    [Symbol.toPrimitive](): number;
    set(newAddress: number | Pointer<any>): this;
    add(amount: number): this;
    /**
     * Cast the pointer to a different type.
     */
    as<C extends Type>(type: C): Pointer<C>;
    deref(): Value<T>;
}
