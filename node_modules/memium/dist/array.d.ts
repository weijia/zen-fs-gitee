import type { InstanceOf, StructConstructor } from './structs.shared.js';
import type { ArrayOf, Type, TypeArrayConstructor, Value } from './types.js';
export declare function useStrictArrayIndexes(): void;
/**
 * The view on memory used for non-primitive array types.
 * This is a *value*
 */
export declare function StructArray<T extends Type, N extends number = number>(type: T, __length?: N): TypeArrayConstructor<Value<T>>;
/**
 * Type used to extract the runtime value type of an `ArrayType`.
 */
export type ArrayValue<T extends Type> = undefined extends T['array'] ? ArrayOf<T extends StructConstructor<any> ? InstanceOf<T> : Value<T>> : InstanceType<T['array'] & (new (...args: any[]) => unknown)>;
/**
 * A class used to create any *type* representing an array of a given "inner" or element type.
 */
export declare class ArrayType<T extends Type = Type> implements Type<ArrayValue<T>> {
    readonly type: T;
    readonly length: number;
    readonly name: string;
    readonly size: number;
    private __structArray;
    private __arrayType;
    /** @internal @hidden */
    readonly __isArrayType = true;
    constructor(type: T, length: number);
    get: (buffer: ArrayBufferLike, offset: number) => ArrayValue<T>;
    set: (buffer: ArrayBufferLike, offset: number, value: ArrayValue<T>) => void;
    /**
     * This is for an array of this array
     */
    array: TypeArrayConstructor<ArrayValue<T>>;
}
